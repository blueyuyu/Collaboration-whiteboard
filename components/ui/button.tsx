import { Button } from '@nextui-org/react';

const StylishButton = () => {
  return (
    <Button
      color="primary"
      size="lg"
      style={{
        background: '$gradient'
      }}
    >
      Stylish Button
    </Button>
  );
};

export default StylishButton;